# Interface graphique (en plus de requirements.txt)
-r requirements.txt
PySide6>=6.5
# Lecture des bons fournisseurs au format PDF (reconstruction du texte via les
# glyphes de la police embarquee). Optionnel : seulement pour l'import PDF.
pikepdf>=8
pdfplumber>=0.10
fonttools>=4.40
